import { ref, Ref } from "vue";
export const title: Ref<string> = ref("");
export const rightText: Ref<string> = ref("");
export const pathValue: Ref<string> = ref("");
export const localStorageList: Ref<unknown[]> = ref([]);
